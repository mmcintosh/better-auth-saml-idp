# The image only maps /simplesaml on its :443 vhost; the e2e runs over plain http on :80.
Alias /simplesaml /var/simplesamlphp/public/
<Directory /var/simplesamlphp/public>
    Require all granted
</Directory>
